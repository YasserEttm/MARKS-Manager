version: '3'
services:
  frontend:
    build:
      context: ./FrontEnd
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  backend:
    build:
      context: ./BackEnd
      dockerfile: Dockerfile
    ports:
      - 8082:8082
